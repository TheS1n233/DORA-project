# English comments only
[project]
name = "assistant-svc"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn>=0.30.0",
  "httpx>=0.27.0"
]

[tool.uvicorn]
factory = false
