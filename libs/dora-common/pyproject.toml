[tool.poetry]
name = "dora-common"
version = "0.1.0"
description = "Shared helpers for DORA micro-services"
authors = ["Team DORA"]

[tool.poetry.dependencies]
python = "^3.11"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
